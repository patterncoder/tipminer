.container.col-md-12
    .breadcrumb {{pageTitle}}
    table.table
        thead
            tr
                th Users
            tr(ng-repeat="user in users")
                td {{user.firstName + ' ' + user.lastName}}
